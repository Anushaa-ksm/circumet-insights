// Content script - runs on every webpage
// Used by popup.js to extract data from the page

// This script doesn't need to do anything actively
// It just needs to exist so the popup can inject code into the page
console.log('CircuMet content script loaded');
